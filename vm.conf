# SPDX-License-Identifier: GPL-2.0-only or BSD-2-Clause
# Copyright 2025 Hewlett Packard Enterprise Development LP
#
# libcxi configuration for the shared vm-tools VM launcher.

VM_MEMORY="${VM_MEMORY:-8G}"
VM_CPUS="${VM_CPUS:-2}"

# Leave VM_DEVICE empty to use the emulated CCN device chosen by netsim.
VM_DEVICE="${VM_DEVICE:-}"
